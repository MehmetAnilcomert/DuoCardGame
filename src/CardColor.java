/**
 * Enum representing the colors of cards in the Duo card game.
 * Contains the values of standard colors (BLUE, GREEN, RED, YELLOW) and a special WILD color.
 */

public enum CardColor {
    BLUE, GREEN, RED, YELLOW, WILD;
}
